# API mode: "local" (direct to e-Stat) or "worker" (through Cloudflare Worker)
API_MODE=local

# Required for local mode only
ESTAT_APP_ID=YOUR_APP_ID_HERE

# Worker URL (for worker mode)
WORKER_URL=http://localhost:8787
